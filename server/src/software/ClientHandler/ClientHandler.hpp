#include <array>
#include <boost/asio.hpp>
#include <cstring>
#include <iostream>
#include <string>

// Класс для обработки запросов от клиентов
class ClientHandler {
  private:
    /* data */
  public:
    ClientHandler(/* args */);
    ~ClientHandler();
};
